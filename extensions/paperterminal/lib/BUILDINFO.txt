PaperTerminal bundled HTTPS stack
=================================

These binaries replace the Kindle 3's stock curl/openssl, whose TLS stack
is far too old for today's HTTPS. They are statically linked (musl libc),
so they run with no dependency on anything in the Kindle firmware.

curl        curl 8.21.0 with OpenSSL 3.5.7 (LTS) linked in statically.
            Protocols restricted to http/https (+ws/wss). Default CA
            bundle path baked in: /mnt/us/extensions/paperterminal/lib/cacert.pem
openssl     OpenSSL 3.5.7 command-line tool (s_client etc. for debugging).
            OPENSSLDIR: /mnt/us/extensions/paperterminal/lib/ssl
cacert.pem  Mozilla CA root certificate bundle as published by the curl
            project (https://curl.se/ca/cacert.pem), 121 roots.

Target
------
ARMv5 EABI soft-float, statically linked with musl 1.2.5 — runs on the
Kindle 3 Keyboard (i.MX35, ARM1136 = ARMv6) and its 2.6.26 kernel.
OpenSSL is configured with --with-rand-seed=devrandom because the 2.6
kernel predates the getrandom() syscall.

Toolchain: Bootlin armv5-eabi--musl--stable-2025.08-1 (Buildroot)
Rebuild from source with: build/build-https-stack.sh (see repo root)

Source integrity at build time
------------------------------
openssl-3.5.7.tar.gz  sha256 verified against publisher (.sha256 file):
  a8c0d28a529ca480f9f36cf5792e2cd21984552a3c8e4aa11a24aa31aeac98e8
cacert.pem            sha256 verified against https://curl.se/ca/cacert.pem.sha256:
  f66dff1bdf8f96060b8177976f8b7d9254bc89bc4db933d769f7384d28480bc9
curl-8.21.0.tar.xz    sha256 recorded at download (no plain hash published;
  the curl project signs releases with GPG instead):
  aa1b66a70eace83dc624508745646c08ae561de512ab403adffb93ac87fc72e6
armv5-eabi--musl--stable-2025.08-1.tar.xz  sha256 recorded at download:
  8cdb4ad70c6b5a66427fa3315fe3ddde1c19c90232674dec59045e04d2a36cf1

Shipped binary hashes are in SHA256SUMS next to this file.

Verification performed
----------------------
- TLS 1.3 and TLS 1.2 handshakes between this curl and this openssl
  (s_server) under qemu-arm, certificate verification enabled.
- Negative test: verification against a non-matching CA bundle fails.
- Real-world HTTPS fetch with certificate verification: HTTP 200.
